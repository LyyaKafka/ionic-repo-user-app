import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root',
})
export class PaginationService {
  constructor() {}

  // i got lazy so i just copy the whole mechanic in homepage page to document page
}
